import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h1></h1>
            <p>Context API is a React Structure that can solve a lot of problems.Context API make particular data available to all components, it never install a state management library in our project that will eventually cost our project performance and increase our bundle size</p>

        </div>
    );
};

export default Blogs;